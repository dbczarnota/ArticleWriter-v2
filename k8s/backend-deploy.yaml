apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: headlinesforge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      initContainers:
        # Run Alembic migrations before the backend starts. The same image is
        # used so any migration files baked into the build are available. The
        # backend pod stays Pending until this container exits 0, which means
        # the API never serves on a stale schema.
        - name: migrate
          image: ghcr.io/dbczarnota/articlewriter-v2/backend:latest
          command: ["uv", "run", "alembic", "upgrade", "head"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: DATABASE_URL
      containers:
        - name: backend
          image: ghcr.io/dbczarnota/articlewriter-v2/backend:latest
          ports:
            - containerPort: 8000
          env:
            - name: AUTH_BACKEND
              value: kinde
            - name: DB_BACKEND
              value: postgres
            - name: CORS_ALLOWED_ORIGINS
              value: "https://app.headlinesforge.com"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: DATABASE_URL
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: GEMINI_API_KEY
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: OPENAI_API_KEY
                  optional: true
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: GROQ_API_KEY
                  optional: true
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: OPENROUTER_API_KEY
                  optional: true
            - name: SERPER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: SERPER_API_KEY
            - name: JINA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: JINA_API_KEY
            - name: HTML2MEDIA_ADMIN_SECRET
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: HTML2MEDIA_ADMIN_SECRET
            - name: KINDE_DOMAIN
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: KINDE_DOMAIN
            - name: KINDE_AUDIENCE
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: KINDE_AUDIENCE
            - name: LOGFIRE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: headlinesforge-secrets
                  key: LOGFIRE_TOKEN
                  optional: true
            - name: SERPER_COST_PER_QUERY_USD
              valueFrom:
                configMapKeyRef:
                  name: articlewriter-config
                  key: SERPER_COST_PER_QUERY_USD
            - name: JINA_COST_PER_PAGE_USD
              valueFrom:
                configMapKeyRef:
                  name: articlewriter-config
                  key: JINA_COST_PER_PAGE_USD
            - name: APIFY_ACTOR_COSTS
              valueFrom:
                configMapKeyRef:
                  name: articlewriter-config
                  key: APIFY_ACTOR_COSTS
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: headlinesforge
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8000
