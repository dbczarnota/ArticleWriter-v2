server {
    listen 80;
    root /usr/share/nginx/html;

    # React SPA handles both landing (/) and app (/app/*) via path-based routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
